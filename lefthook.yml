pre-commit:
  piped: true
  jobs:
    - name: typecheck
      root: site/
      run: pnpm tsc --noEmit

    - name: lint
      root: site/
      glob: "*.{ts,tsx,mts,js,jsx,mjs}"
      run: pnpm eslint {staged_files}
