# Craft CMS GraphQL endpoint (use HTTP for local DDEV — Node rejects self-signed certs)
CRAFT_URL="http://your-craft-site.ddev.site"

# Shared secret for webhook validation (must match cms/.env)
REVALIDATION_SECRET=""
